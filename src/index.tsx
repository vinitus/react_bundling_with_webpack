import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
  return (
    <div>
      <h1>Hello, React App with TypeScript and Webpack!</h1>
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById('root'));
