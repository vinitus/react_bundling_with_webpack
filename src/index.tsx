import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
  return (
    <div>
      <h1>React + TypesSript 프로젝트 webpack으로 번들링하기</h1>
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById('root'));
